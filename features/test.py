# from flask import Flask, Blueprint, render_template, session, redirect, url_for
# from models import AccountInfo


# app = Flask(__name__)

# all_accounts = AccountInfo.query.all()

# print(all_accounts)